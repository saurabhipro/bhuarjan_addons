<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Section 3D (NH) List View -->
    <record id="view_section3d_nh_list" model="ir.ui.view">
        <field name="name">bhu.section3d.nh.list</field>
        <field name="model">bhu.section3d.nh</field>
        <field name="arch" type="xml">
            <list string="Section 3D (NH)" delete="1" create="not groups='bhuarjan.group_bhuarjan_collector'">
                <field name="name" string="Reference"/>
                <field name="department_id" string="Department"/>
                <field name="project_id" string="Project"/>
                <field name="village_id" string="Village"/>
                <field name="create_date" string="Created On"/>
            </list>
        </field>
    </record>

    <!-- Section 3D (NH) Form View -->
    <record id="view_section3d_nh_form" model="ir.ui.view">
        <field name="name">bhu.section3d.nh.form</field>
        <field name="model">bhu.section3d.nh</field>
        <field name="arch" type="xml">
            <form string="Section 3D (NH) / धारा 3डी (राष्ट्रीय राजमार्ग)" 
                  edit="not groups='bhuarjan.group_bhuarjan_collector'"
                  create="not groups='bhuarjan.group_bhuarjan_collector'">
                <sheet>
                    <div class="oe_title" style="margin-bottom: 10px; padding-bottom: 8px;">
                        <h1 style="font-size: 28px; font-weight: 700; color: #8B4513; margin: 0;">
                            <i class="fa fa-file-text-o" style="margin-right: 10px;"></i>Section 3D (NH) / धारा 3डी (राष्ट्रीय राजमार्ग)
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="village_domain" invisible="1"/>
                            <field name="project_domain" invisible="1"/>
                            <field name="department_id" options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="project_id" domain="project_domain or [('id', 'in', [])]" 
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="village_id" domain="village_domain" 
                                   options="{'no_create_edit': True, 'no_create': True, 'no_open': True}"/>
                        </group>
                    </group>
                    <group string="Files / फ़ाइलें">
                        <field name="sdm_signed_file" filename="sdm_signed_filename"/>
                        <field name="sdm_signed_filename" invisible="1"/>
                    </group>
                    <group>
                        <field name="notes" placeholder="Enter any additional notes..."/>
                    </group>
                </sheet>
                <chatter groups="bhuarjan.group_bhuarjan_ri,bhuarjan.group_bhuarjan_nayab_tahsildar,bhuarjan.group_bhuarjan_tahsildar,bhuarjan.group_bhuarjan_sdm,bhuarjan.group_bhuarjan_additional_collector,bhuarjan.group_bhuarjan_collector,bhuarjan.group_bhuarjan_admin"/>
            </form>
        </field>
    </record>

    <!-- Section 3D (NH) Search View -->
    <record id="view_section3d_nh_search" model="ir.ui.view">
        <field name="name">bhu.section3d.nh.search</field>
        <field name="model">bhu.section3d.nh</field>
        <field name="arch" type="xml">
            <search string="Section 3D (NH)">
                <field name="name"/>
                <field name="department_id"/>
                <field name="project_id"/>
                <field name="village_id"/>
            </search>
        </field>
    </record>

    <!-- Section 3D (NH) Action -->
    <record id="action_section3d_nh" model="ir.actions.act_window">
        <field name="name">Section 3D (NH)</field>
        <field name="res_model">bhu.section3d.nh</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_section3d_nh_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new Section 3D (NH) record!
            </p>
        </field>
    </record>
</odoo>
